# fanwaave-cli

Rust CLI for Fan and push-notification delivery across web, mobile, and desktop. Uses flags-2-env via `.cli-flags.toml`. Talks to `fanwaave-api-server.rs`.
Env keys are generated by `f2e generate` from `.cli-flags.toml` into `generated/{rust,dart,typescript,gleam}`.
